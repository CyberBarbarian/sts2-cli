# 本地敏感配置模板。
# 复制为 .env 后填写真实 key；.env 会被 git 忽略。
# 非敏感 benchmark 参数请修改 sts2_bench/benchmark.yaml。

# DeepSeek API key。使用 DeepSeek 时填写这一项。
DEEPSEEK_API_KEY=

# OpenAI 或其他兼容服务的 API key。通常不需要；当未设置 DEEPSEEK_API_KEY 时会作为 fallback。
# OPENAI_API_KEY=
